"""Real-data adapter STUBS: clean interfaces, no downloads, no claims.

Each adapter documents the real data source it would wrap and raises
NotImplementedError. The rest of Emberline consumes only the synthetic
worldgen; swapping in these adapters (same array contracts) is the
sim-to-real path described in REPORT.md.
"""
